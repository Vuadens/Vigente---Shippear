{
  "name": "vigente",
  "private": true,
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "pipeline": "pnpm --filter @vigente/pipeline extraer",
    "typecheck": "pnpm -r typecheck"
  }
}
